export class Ingredients{
//public name: string;
//public amount: number; alternatively can directly add public in cnstructor
constructor(public name: string, public amount: number){
//this.name=name;
//this.amount=amount;
}
}